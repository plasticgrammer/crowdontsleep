{
  "name": "line-reminder-bot",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "deploy": "sam build && sam deploy --guided"
  },
  "dependencies": {
    "@line/bot-sdk": "^7.5.2",
    "aws-sdk": "^2.1400.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^16.18.38",
    "typescript": "^4.9.5",
    "aws-sam-cli": "^1.86.1"
  }
}
